# jvm heap for the gradle daemon
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# this app deliberately uses only platform apis, so androidx is off
android.useAndroidX=false
org.gradle.caching=true
